server:
  port: 8080

spring:
  application:
    name: pet-adoption-shelterserver
  profiles:
    default: dev
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false

springdoc:
  api-docs:
    path: /api-docs