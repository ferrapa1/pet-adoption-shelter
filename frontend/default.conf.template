server {
  listen 80;

  # Serve the Angular application
  location / {
    root /usr/share/nginx/html;
    index index.html index.htm;
    try_files $uri $uri/ /index.html;
  }

  # Reverse proxy for API calls
  location /api {
    # Use Docker's internal DNS resolver
    resolver 127.0.0.11 valid=30s;

    # This value will be substituted by the NGINX_PROXY_PASS environment variable
    proxy_pass ${NGINX_PROXY_PASS};

    # Set the Host header to the backend's host, not the original request's host
    proxy_set_header Host $proxy_host;

    # These headers help the backend know about the original request
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
